apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: kibana-workload-auth
  name: kibana-workload-auth
  namespace: tectonic-system
spec:
  ports:
  - name: http
    port: 4180
    protocol: TCP
    targetPort: http
  selector:
    k8s-app: kibana-workload-auth
  sessionAffinity: None
  type: ClusterIP
